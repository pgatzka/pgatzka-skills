{
  "description": "Enforce structured-questions rules at the AskUserQuestion tool boundary. Blocks AskUserQuestion calls that lack a (Recommended) option or literal Pros:/Cons: lines per option, with an exception for whitelisted yes/no pairs.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "AskUserQuestion",
        "hooks": [
          {
            "type": "command",
            "command": "python ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/validate_ask_user_question.py",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
